apiVersion: v1
kind: Secret
metadata:
  name: photosafe-secrets
  namespace: photosafe
  labels:
    app.kubernetes.io/name: photosafe
    app.kubernetes.io/part-of: photosafe
type: Opaque
stringData:
  # IMPORTANT: Replace these values before deploying.
  # In production, use sealed-secrets, external-secrets, or your cloud
  # provider's secret management instead of checking values into git.
  POSTGRES_PASSWORD: "CHANGE_ME"
  PHOTOSAFE_PASSWORD: "CHANGE_ME"
  SECRET_KEY: "CHANGE_ME"
  # Optional: S3 / AWS credentials
  # AWS_ACCESS_KEY_ID: ""
  # AWS_SECRET_ACCESS_KEY: ""
  # Optional: Apple credentials encryption key
  # APPLE_CREDENTIALS_ENCRYPTION_KEY: ""
